# THE STANDARD (frozen 2026-07-30)

A claim exists only if it passes S1-S4. It is ESTABLISHED only if it also
passes S5-S6. It stays alive only while S7 holds. No exceptions, including
for the author, especially for the author.

S1 PROVENANCE   Every number regenerates from bytes on disk by one script.
                No script, no number.
S2 TIER LABEL   ARITH / EXACT / FIT, stated. A FIT without its window and
                gate protocol encoded in the script does not exist.
S3 EXACT ANCHOR Every observable passes at least one zero-free-parameter
                identity test (symmetry, invariant) before any trend in it
                is read.
S4 IDENTITY     Every sup or fit certifies WHICH object it measured
                (co-location or decomposition), not merely that it converged.
S5 TWO STREAMS  Established requires two disjoint methods. One stream =
                ABDUCED, said once, plainly, then work continues.
S6 ADVERSARIAL  A claim is spoken as a result only after a confound pass;
                surviving OPEN or FATAL rows block promotion.
S7 ENFORCEMENT  Certified claims live in verify.py under the watcher.
                Regression suspends the claim automatically.

## The ledger, graded against the standard. Final.

ESTABLISHED (S1-S6 pass)
  sigma_Lambda(inviscid) = +1.00 +- 0.03   EXACT x2 (symmetry 3e-4; factorial
                                           encoded), decomposition done, watcher-held
  Theorem 0 (Batchelor cap)                proof unconditional; cap verified on
                                           data with margin
  alpha_0 = -0.34240                       two disjoint methods (ours + published)
  Branch arithmetic (ratios, alpha_inf)    ARITH, in verify.py

ESTABLISHED GIVEN NAMED HYPOTHESES
  Theorem 1 (sigma < -1/2 kills type-I)    H1 type-I; H2 measured lambda_0 = 5.0;
                                           chain verified 21/21 on data
  Theorem 2a/2b (endless calc, mode        given collapse law c_l
  replacement)

ABDUCED (one stream; needs its second)
  c_l = -1/alpha physical identification   matching derivation, single pass
  Viscous inversion sigma_PEAK < 0         single grid 128x384; needs 256x768
  Branch family closed (alpha_inf)         rests on published alpha_1..3
  Knife-edge condition (Thm 2d)            conditional on (A)

DEAD (failed S6; stays dead)
  Reciprocal branch lambda = 1/3
  Batchelor saturation gloss
  Collision separation-dependence          (all three sweeps confounded)

NO VERDICT PERMITTED
  Collision hypothesis itself              never cleanly tested
  sigma in the strip [-1/2, 0] for NS      the open problem

## Directive, standing

Report only what passes. Grade before speaking. One correction per error,
then silence on it. The watcher, not the narrator, holds the record.
